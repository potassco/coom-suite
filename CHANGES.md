# Changes

## v1.1.0

- added support for parsing and solving simple defaults
- added support for associations (COOM `reference` keyword)
- COOM `imply` statements are handled like assignments in encoding instead of
  being converted to Boolean constraints

## v1.0.0

- initial Release
